# Combined workflow for CI, validation, Docker builds, and releases
name: Main

on: [push]

permissions:
  contents: write
  packages: write
  pull-requests: write
  id-token: write
  attestations: write

jobs:
  test:
    name: Elixir CI (Elixir ${{ matrix.elixir }} / OTP ${{ matrix.otp }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        elixir: ['1.17', '1.18', '1.19']
        otp: ['25', '26', '27', '28']
        exclude:
          - { elixir: '1.17', otp: '28' }
          - { elixir: '1.18', otp: '28' }
          - { elixir: '1.19', otp: '25' }
    steps:
      - uses: actions/checkout@v3
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ matrix.elixir }}
          otp-version: ${{ matrix.otp }}

      - name: Cache Mix dependencies
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-mix-${{ matrix.elixir }}-${{ matrix.otp }}-

      - name: Install dependencies
        run: mix deps.get

      - name: Check code format
        run: mix format --check-formatted

      - name: Run Credo
        run: mix credo --strict

      - name: Run tests
        run: mix test

      - name: Run Dialyzer
        run: mix dialyzer --format github

  validate-blocknote:
    name: Validate BlockNote Snapshots
    runs-on: ubuntu-latest
    env:
      DENO_VERSION: "~2.5"
    steps:
      - uses: actions/checkout@v3

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: ${{ env.DENO_VERSION }}

      - name: Cache Deno dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/deno
          key: ${{ runner.os }}-deno-${{ env.DENO_VERSION }}-${{ hashFiles('.ci/deno.lock') }}
          restore-keys: |
            ${{ runner.os }}-deno-${{ env.DENO_VERSION }}-

      - name: Install Deno dependencies
        run: deno install --frozen
        working-directory: .ci

      - name: Validate BlockNote snapshots
        run: deno run --allow-env --allow-read .ci/main.ts ./test/snapshots/BlockNote.Writer/*/expected.json

  docker:
    name: Docker Build and Push
    runs-on: ubuntu-latest
    needs: [test, validate-blocknote]
    steps:
      - uses: actions/checkout@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - id: meta
        uses: docker/metadata-action@v4
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=ref,event=branch,filter=branch=main,format=latest
            type=ref,event=tag

      - name: Build (always) and push (only on tags)
        uses: docker/build-push-action@v6
        id: build-and-push
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ startsWith(github.ref, 'refs/tags/') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install Cosign
        if: startsWith(github.ref, 'refs/tags/')
        uses: sigstore/cosign-installer@v3
        with:
          cosign-release: "v2.4.0"

      - name: Sign the images with GitHub OIDC Token
        if: startsWith(github.ref, 'refs/tags/')
        env:
          DIGEST: ${{ steps.build-and-push.outputs.digest }}
          TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          set -euo pipefail

          images=""
          for tag in $TAGS; do
            images+="${tag}@${DIGEST} "
          done

          echo "Signing these images: $images"
          cosign sign --yes $images

  release-please:
    name: Release Please
    runs-on: ubuntu-latest
    needs: [test, validate-blocknote, docker]
    # Only run on main branch pushes after all other jobs succeed
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
